# HTML pages assembled from Stitch blocks

Files in this folder are composed from `design/html_blocks/*/block.html`
and include shared dependencies in the head.
Note: some blocks are inserted as main content only (header/footer stripped).
