# Активный контекст — MGTS B2B

Обновлено: **2026-08-19** · слот `arch` · ветка `main` · лимит файла: 60 строк

## Что за проект
Новый B2B-сайт МГТС. Strapi 5.36 (`mgts-backend/`) + Next.js 16 (`mgts-frontend/`) +
легаси-статика `SiteMGTS/`. Репо `github.com/toycenterboss-bot/mgts-b2b`, локально `~/Downloads/runs`.

**Идентичность (стратегия 2027–2029): МГТС — UrbanTech-оператор Москвы.**
Дивизионы: PropTech (создание) и InfraTech (аренда). Подробно — `topics/strategy-2027-2029.md`.

## Где мы (19.08)
Идёт **редизайн**. Strapi :1337 и фронт :3000 подняты, 216 страниц целы.
**Ф0 ЗАКРЫТА**, **Ф1.5 базовый прогон снят**, идёт **Ф1**. Смержено 12 PR,
замков снято **17 из 33**, тестов 35. Фаз стало 10: заведена **Ф1.6 «Адаптив»**.
Табло — `PLAN-01` §0.0, детали Ф0 — `sessions/2026-08/2026-08-18-ф0.md`.

**Дизайн-ревью сделано** (`docs/design-review/`): 5 отчётов Claude Design + прибор
`design-metrics.mjs` + реестр **32 дефектов** с приборами и фазами + ворота
`check-defects.mjs`. Дефект закрывается числом либо `коммит:<sha>` — третьего нет.

🔴 Что нашло ревью: мобильной вёрстки нет **ни на одной из 100 страниц** (документ
668 px в окне 390) · 2470 узлов ниже AA · 206 `!important` · 1143 литерала цвета ·
7624 иконочных слота из 7631 идут аварийным путём, 244 нарисованных SVG не нужны
никому · 10 опубликованных страниц с пустым телом · 17 блоков текста-рыбы в проде.

Артефакты редизайна: `design/geo/`, `design/prototypes/`, `docs/redesign/`,
`docs/design-review/`. 🔴 Справочники районов соединяются только по названию.

## Что брать следующим
1. **Ф1 «Тема»** — идёт. Токены в `globals.css`, `!important` **206 → 0**,
   узлов ниже AA **2470 → 0**, `:focus-visible` **0 → есть**, шрифт через `next/font`.
   Дефекты Д-01…Д-08, Д-31.
2. **Ф1.6 «Адаптив»** — мобильная шапка, обрезка `light-leak`, цели 44 px,
   skip-link, метки полей. Дефекты Д-09…Д-13, Д-30.
3. **Ф2 «Каркас CMS»** — B-11 → 17 компонентов → webhook; один источник иконок
   и компоненты Button/SearchField/EmptyState (Д-14…Д-19).

🔴 CSS отдаётся route-обработчиком от `process.cwd()` — деплой одного фронта
оставит сайт без стилей (B-35). 🔴 **Q-R01.9**: 3D-рендер несёт Ф5 (B-29).
Заглушка показывает форму ответа; цифры и цитаты в `stub` запрещены машиной.

## Что горит из старого
🟡 **B-01** секреты в истории публичного репо · ✅ **B-02** роуты закрыты · 🟡 **B-03** схема воспроизводится, контент нет (B-34).
## Четыре ловушки (подробно — PLAN-01 §4)
`PageRenderer.tsx` не трогать · `default: return null` прячет секции молча ·
геометрия не в бандл, `revalidateTag` не зовётся · заглушка тихо становится фактом.

## Чего здесь НЕТ
Знания → `topics/` · слот → `context/arch.md` · задачи → `backlog.md` ·
находки → `inbox/2026-08.md` · план → `implementation-plans/PLAN-01`.

## Ритуалы
- **Планировать по `PLANNING-STANDARD.md`:** у фазы Артефакт · Метрика ·
  Инвариант (класс) · Откат; при закрытии — блок с «что НЕ доказано».
- **Начало:** `sh memory-bank/check-memory.sh`, затем `index.md`, затем этот файл.
- **Конец:** запись в `inbox/`, обновление этого файла, `git add` поимённо, commit, **push**.
- Секция 8 краснеет, если `docs/`, `design/`, `src/`, `scripts/` правились позже
  этого файла. ⚠️ git по этому репозиторию — только через **Desktop Commander**
  (`topics/agent-environment.md`).
