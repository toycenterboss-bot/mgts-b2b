# Активный контекст — MGTS B2B

Обновлено: **2026-08-14** · слот `arch` · ветка `main` · лимит файла: 60 строк

## Что за проект
Новый B2B-сайт МГТС. Три слоя: легаси-статика `SiteMGTS/` (источник контента) →
Strapi 5.36 (`mgts-backend/`) → Next.js 16 App Router (`mgts-frontend/`).
Репозиторий: `github.com/toycenterboss-bot/mgts-b2b` · локально `~/Downloads/runs`.

## Где остановились (замер по коду 2026-08-14)
- Последний коммит: `932e20e`, **2026-03-13**. Содержательной разработки нет ~5 месяцев.
- Последний замер качества: `docs/project/visual-compare/report.md` (2026-03-03) —
  118 страниц, расхождение с эталоном **1.5–34.6 %**, паритет не достигнут ни на одной.
- Мемори-банк заведён **2026-08-14** (эта сессия). Поколение A из `docs/` вынесено
  в `_to_delete/`, знания выжаты в `topics/`.

## Три вещи, которые горят (подробно — `backlog.md`)
1. 🔴 **B-01** Боевые секреты в git-истории: Perplexity key
   (`docs/project/CONTEXT.md:17,22`), Strapi API token
   (`docs/project/CHAT_LOG_2026_02_09_Cards_Issue.md:166225`), куки `.mgts.ru`
   в 455 файлах Chrome-профиля. Перенос файлов в `_to_delete/` историю **не чистит**.
2. 🔴 **B-02** `DELETE /api/pages/delete-all` с `auth: false`
   (`mgts-backend/src/api/page/routes/custom-page.ts:50-58`) + 5 публичных POST.
3. 🔴 **B-03** Ноль миграций БД (`mgts-backend/database/migrations/` = только
   `.gitkeep`) при 8 типах и 72 компонентах; `.tmp/data.db` вне репозитория.
   Свежий клон **не разворачивается**. Тип `api::icon.icon` восстановить неоткуда.

## Что брать следующим
- **B-01** — до всего остального: риск растёт со временем, ключи живые.
- **B-04** (Tailwind не собирается из `mgts-frontend/src/**`) — это корневая причина
  визуальных расхождений, а не «недоделанная вёрстка». Разбор — `topics/frontend-render.md`.
- **B-03** — без него любой новый участник не может поднять проект.

## Чего в этом файле НЕТ и быть не должно
Знания «как устроено» → `topics/`. Оперативка слота → `context/arch.md`.
Очередь задач → `backlog.md`. Найденные ошибки → `inbox/2026-08.md`.

## Ритуалы
- **Начало сессии:** `sh memory-bank/check-memory.sh`, затем `index.md`, затем этот файл.
- **Конец сессии:** запись в `inbox/`, обновление этого файла, `git add <файлы поимённо>`,
  commit, **push**.
