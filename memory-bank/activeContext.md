# Активный контекст — MGTS B2B

Обновлено: **2026-08-17** · слот `arch` · ветка `main` · лимит файла: 60 строк

## Что за проект
Новый B2B-сайт МГТС. Strapi 5.36 (`mgts-backend/`) + Next.js 16 (`mgts-frontend/`) +
легаси-статика `SiteMGTS/`. Репо `github.com/toycenterboss-bot/mgts-b2b`, локально `~/Downloads/runs`.

**Идентичность по стратегии 2027–2029: МГТС — UrbanTech-оператор Москвы.**
Два дивизиона на сайте: PropTech (создание) и InfraTech (аренда). MTS Ops — не для сайта.
Подробно — `topics/strategy-2027-2029.md`.

## Где остановились (17.08.2026)
Идёт **редизайн**. Концепция принята владельцем после 7 итераций, прототипы сделаны
и доставлены файлами. Планы интеграции, валидации и требований написаны.
Ни строчки кода в репозиторий пока не легло — см. `implementation-plans/PLAN-01`, табло §0.0.

Главный замер: **DZ 29 = свитч 29** — контракт «схема ↔ рендер» целый, сотню существующих
страниц трогать не придётся.

**Проверка готовности плана (17.08):** к старту не готов — из 11 шагов запускаются 4.
Четыре блокера в `PLAN-01 §0.1`: нет входа у трёх шагов, открытые пишущие эндпоинты,
DoD темы с перевёрнутым знаком, нет тест-раннера. В план добавлен шаг 0.

## Что брать следующим
1. **Шаг 0 из PLAN-01 §5** — снятие блокеров, ~4 дня.
2. **B-04, сборка Tailwind** — предусловие ко всему остальному.
3. **Токены + светлая тема без `!important`** — 80 % визуального эффекта на всех
   100 страницах, ноль правок контента.

## Что горит из старого
🔴 **B-01** боевые секреты в git-истории · 🔴 **B-02** публичный `DELETE /api/pages/delete-all`
🔴 **B-03** ноль миграций БД, проект не разворачивается с нуля.
Ни одна не закрыта с 14.08.

## Три ловушки редизайна (подробно — PLAN-01 §2)
`PageRenderer.tsx` не трогать · `default: return null` прячет секции молча ·
геометрия не в бандл + `revalidateTag` не вызывается нигде.

## Чего в этом файле НЕТ
Знания → `topics/`. Оперативка слота → `context/arch.md`. Задачи → `backlog.md`.
Находки → `inbox/2026-08.md`. План редизайна → `implementation-plans/PLAN-01`.

## Ритуалы
- **Начало:** `sh memory-bank/check-memory.sh`, затем `index.md`, затем этот файл.
- **Конец:** запись в `inbox/`, обновление этого файла, `git add` поимённо, commit, **push**.
- Секция 8 проверки ругается, если `docs/`, `design/`, `src/` или `scripts/` правились
  позже этого файла. Не «не забудь записать», а красный экран. Мутации пройдены 17.08.
- ⚠️ git по этому репозиторию — через **Desktop Commander**, не через `device_bash`.
  Почему — `topics/agent-environment.md`.
