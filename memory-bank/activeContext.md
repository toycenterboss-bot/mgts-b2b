# Активный контекст — MGTS B2B

Обновлено: **2026-08-19** · слот `arch` · ветка `main` · лимит файла: 60 строк

## Что за проект
Новый B2B-сайт МГТС. Strapi 5.36 (`mgts-backend/`) + Next.js 16 (`mgts-frontend/`) +
легаси-статика `SiteMGTS/`. Репо `github.com/toycenterboss-bot/mgts-b2b`, локально `~/Downloads/runs`.

**Идентичность (стратегия 2027–2029): МГТС — UrbanTech-оператор Москвы.**
Дивизионы: PropTech (создание) и InfraTech (аренда). Подробно — `topics/strategy-2027-2029.md`.

## Где мы (19.08)
Идёт **редизайн**. Strapi :1337 и фронт :3000 подняты, 216 страниц целы.
**Ф0 ЗАКРЫТА** (закрывающий блок в `PLAN-01`), идёт **Ф1**. Смержено 10 PR,
замков снято **5 из 21**, тестов **35**. Замер контракта: **DZ 29 = свитч 29** —
сотню страниц трогать не придётся.
**PLAN-01 по стандарту:** 9 фаз, у каждой Артефакт · Метрика · Инвариант · Откат;
нет вводной — заглушка, а не пауза (§2.2). Табло — `PLAN-01` §0.0,
детали Ф0 — `sessions/2026-08/2026-08-18-ф0.md`.

Артефакты редизайна в репозитории: `design/geo/`, `design/prototypes/`, `docs/redesign/`.
🔴 Справочники районов не соединяются по координатам — только по названию
(`design/geo/README.md`).

## Что брать следующим
1. **Ф1 «Тема»** — B-04 закрыт (PR #10, 44 класса без стилей → 0). Дальше токены
   в `globals.css` и светлая тема **без `!important`**: сейчас их 181 на 689 строк.
2. **Ф1.5 «Дизайн-ревью»** — новая фаза по требованию владельца 19.08: пять отчётов
   навыками Claude Design, базовый прогон и повторный на стенде. Разница между
   ними — доказательство, что дизайн улучшился.
3. **Ф2 «Каркас CMS»** — B-11 (68 дублей) → перенос 17 компонентов → webhook.

🔴 **Найдено в PR #10 и не закрыто:** CSS отдаётся route-обработчиком, читающим
`../design/assets` от `process.cwd()`. Деплой одного фронта оставит сайт без стилей —
угроза Ф7.

Заглушка показывает форму ответа; цифры и цитаты в `stub` запрещены машиной.
**CMS:** `design/strapi-new/`, 17 компонентов. 🔴 **Q-R01.9**: 3D-рендер — несущая
конструкция Ф5, оценить до старта (B-29).

## Что горит из старого
🟡 **B-01** секреты вычищены из HEAD, но остаются в истории публичного репо · ✅ **B-02** роуты закрыты · 🟡 **B-03** схема воспроизводится, контент нет (B-34).

## Четыре ловушки (подробно — PLAN-01 §4)
`PageRenderer.tsx` не трогать · `default: return null` прячет секции молча ·
геометрия не в бандл, `revalidateTag` не зовётся · заглушка тихо становится фактом.

## Чего здесь НЕТ
Знания → `topics/` · слот → `context/arch.md` · задачи → `backlog.md` ·
находки → `inbox/2026-08.md` · план → `implementation-plans/PLAN-01`.

## Ритуалы
- **Планировать по `PLANNING-STANDARD.md`:** у фазы Артефакт · Метрика ·
  Инвариант (класс) · Откат; при закрытии — блок с «что НЕ доказано».
- **Начало:** `sh memory-bank/check-memory.sh`, затем `index.md`, затем этот файл.
- **Конец:** запись в `inbox/`, обновление этого файла, `git add` поимённо, commit, **push**.
- Секция 8 проверки ругается, если `docs/`, `design/`, `src/` или `scripts/` правились
  позже этого файла. Не «не забудь записать», а красный экран. Мутации пройдены 17.08.
- ⚠️ git по этому репозиторию — через **Desktop Commander**, не через `device_bash`.
  Почему — `topics/agent-environment.md`.
