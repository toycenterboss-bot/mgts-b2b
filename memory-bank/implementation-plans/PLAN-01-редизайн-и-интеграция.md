---
status: детализирован (заведён 17.08.2026)
priority: 🔴 P0
topics: [architecture, strapi-schema, frontend-render, design-system, moscow-map, strategy-2027-2029]
связи: ../PLANNING-STANDARD.md · ../patterns/coverage.md · design/strapi-new/README.md
---

# План 01 · Редизайн сайта и интеграция в Strapi

Дата: 17.08.2026 | Статус: детализирован | Отменяет: PLAN-01 от 17.08.2026 (плоский порядок работ)

Оформлен по `../PLANNING-STANDARD.md`. Развёрнутые версии доставлены владельцу файлами:
`ПЛАН-интеграции-в-Strapi.md`, `ВАЛИДАЦИЯ-страниц-и-пробелы.md`,
`ТРЕБОВАНИЯ-карта-точки-карточки-3D.md`, `ПЛАН-скоринг-профиля-посетителя.md`.

---

## 0.0 📊 ТАБЛО СОСТОЯНИЯ

**Дата последней сверки с реальностью: 17.08.2026**, по коду репозитория.

### Счётчики

| Ось | ✅ работает | 🟡 частично | ⬜ нет | Всего |
|---|---:|---:|---:|---:|
| Фазы Ф0–Ф7 + Ф1.5 + Ф1.6 | 1 | 2 | 7 | 10 |
| Замки (мутации §9) | 19 | 0 | 16 | 35 |
| Артефакты подготовки | 2 | 4 | 5 | 11 |
| Вопросы Q-R01 | закрыто 8 | — | открыт 1 | 9 |
| Дефекты дизайна (§11) | 0 | 0 | 32 | 32 |

### Фазы

| Код | Название | Статус | Чем закрыт / чего не хватает |
|---|---|---|---|
| Ф0 | Предохранители | ✅ | **закрыта 19.08**, коммит `06a08ff`, замков снято 5 |
| Ф1 | Тема на 118 страницах | 🟡 | 🔴 **ЗАКРЫТИЕ ОТОЗВАНО 19.08**: владелец открыл страницу и увидел регрессию, которой прибор не видел |
| Ф1.5 | Дизайн-ревью | 🟡 | базовый прогон снят 19.08: 5 отчётов, 32 дефекта в реестре, 11 замков; повторный — в Ф7 |
| Ф1.6 | Адаптив и попадание пальцем | ⬜ | мобильной вёрстки нет ни на одной из 100 страниц; сюда же переехали шкала кегля, тени и три находки Ф1 |
| Ф2 | Каркас CMS | ⬜ | ждёт B-11 — снос 68 дублей схем |
| Ф3 | Новая главная | ⬜ | ждёт Ф2 |
| Ф4 | Продукты InfraTech | ⬜ | ждёт Ф3 |
| Ф5 | Кейсы и карта кейсов | ⬜ | ждёт Ф4 и B-29 (оценка 3D-рендера) |
| Ф6 | Живые действия | ⬜ | ждёт Ф5 |
| Ф7 | Стенд и приёмка | ⬜ | ждёт Ф6; площадки не существует (B-25) |

### Артефакты подготовки

| Что | Статус | Чем закрыт / чего не хватает |
|---|---|---|
| Дизайн-концепция | ✅ | принята владельцем, 7 итераций |
| Валидация 118 страниц | ✅ | 82 остаются, 21 переезжает, 2 пересобрать, 13 снять |
| Схемы CMS | 🟡 | 17 компонентов + 3 типа в `design/strapi-new/`, битых ссылок 0; **в `mgts-backend` не перенесены** |
| Прототипы | ✅ | в `design/prototypes/`, три файла, README с границами применимости |
| Требования к карте и 3D | 🟡 | написаны; 9 вопросов закрыты заглушками, состояние 3D-ветки неизвестно (B-29) |
| Скоринг профиля | 🟡 | план есть, аналитики на сайте нет вообще |
| Механизм заглушек | 🟡 | правила, ворота и реестр готовы; данных для разметки нет до Ф2 |
| Сборка Tailwind (B-04) | ⬜ | предусловие ко всему визуальному |
| Тест-раннер (B-26) | ✅ | vitest, 32 теста, 4 файла |
| Токены в коде | ✅ | `design/tailwind/tokens.css`, две темы, обе измерены |
| Площадка для показа (B-25) | ⬜ | 🔴 всё живёт на `localhost` |

### 🔴 «Мера есть» ≠ «проверка работает»

Ни один замок из §9 не проверен мутацией — поэтому в счётчиках **0 ✅ из 19**,
хотя часть мер описана детально. Статус ✅ ставится только после подсадки инцидента.

### Что брать следующим

1. **Ф1 «Тема»** — начинать с **B-04**, сборки Tailwind: пока конфиг не сканирует
   `mgts-frontend/src`, любая новая вёрстка так же потеряет классы. Затем полный
   прогон `visual-baseline capture` на 100 страницах — это базовая линия фазы.
2. **B-11 (68 дублей схем)** — параллельно Ф0. Это предусловие Ф2, а не часть его:
   единственная необратимая операция всего плана.
3. **B-29 (оценка 3D-рендера)** — разведка, а не фаза. После ответа по Q-R01.7 рендер
   стал несущей конструкцией Ф5; вердикт «годен / нужна замена» нужен до старта Ф5,
   а не внутри него.

---

## 0. Факты (замер 17.08.2026)

Заземление на реальность. Всё ниже — по коду репозитория, не по памяти.

| Что мерили | Чем | Результат |
|---|---|---|
| Контракт «схема ↔ рендер» | обход `page.schema.json` и `SectionRenderer.tsx` | **DZ 29 = свитч 29**, совпало 29 из 29 |
| Мёртвые схемы | `find src/components -name schema.json` | **68 дублей** `<name>/schema.json` (B-11) |
| Сборка Tailwind | `design/tailwind/tailwind.config.cjs:3` | сканирует только `../html_blocks/**/*.html`, фронт не видит |
| Утилиты в бандле фронта | `tailwindcss@4` без `@import "tailwindcss"` | **0 утилит генерируется** |
| Прибор визуального сравнения | `visual-compare.js` | сравнивает React со **старой статикой**: медиана 7,03 %, p75 14,48, p90 17,75, макс 34,65 (`/search`), мин 1,55 |
| Пишущие роуты | `src/api/page/routes/custom-page.ts` | `DELETE /pages/delete-all`, `POST /pages/update-parent-relations` и **5** `seed-*` — все `auth: false` |
| Тест-раннер | `mgts-frontend/package.json` | ни `vitest`, ни `jest`, ни скрипта `test` |
| Аналитика | `grep -E "ym\(|gtag\(|dataLayer|matomo"` по фронту | **0 совпадений** |
| Инвалидация кэша | `grep -r revalidateTag` | не вызывается **нигде**, webhook отсутствует |
| Страницы | обход карты сайта | 118: **82** остаются · **21** переезжают · **2** пересобрать · **13** снять |
| Пробелы контента | сверка со стратегией 2027–2029 | 10; **3 из 5 высокоприоритетных — InfraTech**, дающие 86 % прироста в агрессивном сценарии |
| Регуляторные тексты | `visual-compare` по страницам | `/cookie_processing` 24,1 %, `/operators/nondiscriminatory_access` 23,1 % |
| Геометрия | `moscow.json` | 80 КБ, 125 районов, источник OSM/ODbL |
| Площадка показа | `visual-compare.js:8-10` | `STRAPI_BASE`, `REACT_BASE`, `STATIC_BASE` — **три `localhost`**, конфигов деплоя нет |
| Схемы редизайна | обход `design/strapi-new/` | 17 компонентов, 3 типа, 28 внутренних ссылок, **битых 0** |

🔴 **Прибор смотрел мимо (К-03).** Редизайн уводит от старой статики намеренно —
расхождение с ней **вырастет, и это будет успехом**. Эталон переставлен
на «React до → React после»; без этого DoD темы измерялся с перевёрнутым знаком.

---

## 1. Цель и критерий успеха

**Цель.** Новая версия сайта: посетитель за один экран понимает, кто такой МГТС,
опознаёт себя и уходит в PropTech, в InfraTech или в кабинет; сайт отвечает
стратегии 2027–2029 и выглядит дорого и технологично.

**DoD верхнего уровня — пять чисел, не мнения:**

| № | Критерий | Как меряется | Сейчас | Цель |
|---|---|---|---|---|
| К1 | Все 118 страниц живы после смены темы | сдвигов геометрии блоков против снимка «до» | — | **0** |
| К2 | Ни одна секция не исчезает молча | тест «DZ = свитч» + мутация | теста нет | **зелёный; красный на мутации** |
| К3 | Денежное направление представлено | страниц InfraTech-продуктов | **0** | **3** |
| К4 | Развилка ведёт не в тупик | форм с `action="#"` | все | **0** |
| К5 | Ни одна заглушка не уехала в прод | не-`approved` блоков в прод-сборке | — | **0, сборка падает** |

**Не является критерием:** «стало красивее». Оценка коммерческого директора —
**приёмка**, а не метрика; снимается на стенде в Ф7.

---

## 2. Метод и подход

**Тема поверх контракта, а не переписывание.** DZ 29 = свитч 29 — контракт цел.
82 страницы меняются токенами, без единой правки контента: 80 % визуального эффекта
в первой же фазе.

*Чем плохи альтернативы.* Переписать `PageRenderer` — 739 строк каскада `if`
с недостижимыми ветками, риск уронить сотню страниц ради нуля пользы для посетителя.
Собрать новый сайт рядом — два источника правды и вечная миграция контента.

**Исполнитель — агент, а не команда.** Отсюда:

1. Человеко-дни не единица. Фаза закрывается, когда метрика зелёная и мутация поймана.
   Оценки ниже — в сессиях, ориентировочно.
2. Ожидание владельца — не пауза, а заглушка (§2.2). Раньше шесть фаз из восьми стояли
   на «входе от владельца»; теперь ни одна.

**Каждая мера проверяется машиной.** Текстовое правило не исполняется — установлено
на этом же проекте и уже стоило одного разбора.

### 2.1 Карта потребителей (план сквозной)

Где значение читается, где складывается и где **молча предполагается**. Последняя
колонка — то, ради чего карта и рисуется: невидимые допущения ломаются тише всего.

| Узел | Читает | Складывает | Молча предполагает |
|---|---|---|---|
| `page.schema.json` (DZ, 29 → 40) | — | состав секций страницы | что `populate=*` работает; в Strapi 5 **нет**, нужны фрагменты `on` |
| `SectionRenderer.tsx` | DZ | разметку секции | что имя компонента совпало; иначе `default: return null` |
| `PageRenderer.tsx` | шаблон страницы | каркас 15 шаблонов | недостижимые ветки после `return` на `:256` |
| `globals.css` + Tailwind | классы из разметки | визуальный слой | что классы вообще сгенерированы; сейчас **не сгенерированы** |
| ISR (`force-cache`, 300 с) | ответ Strapi | кэш страницы | что кто-то дёргает `revalidateTag`; **никто** |
| `visual-compare` | React и статику | отчёт расхождений | что статика — эталон; после редизайна **уже нет** |
| Админка Strapi | схемы | контент редакторов | что удалить контент анонимно нельзя; **можно** |
| `api::geo` | геометрию | 80 КБ JSON | что геометрия не в бандле; обеспечивается `next/dynamic` |
| Формы | ввод посетителя | заявку | что есть адресат; `action="#"` |
| Аналитика | события | статистику | что счётчик стоит; **не стоит** |

### 2.2 Механизм заглушек — как убирается ожидание

Приём, уже применённый к цифрам (`verified` + `source`) и площадкам
(`sitePrecision: district`), поднят до уровня всего плана.
Реализация — `meta.stub`, см. `design/strapi-new/README.md`. Задача — **B-27**.

**Правило.** Нет вводной — заглушка, а не пауза. Заглушка обязана быть
**видимой в данных, видимой на стенде и невозможной в проде**.

| Статус | Что означает | Как выглядит на странице |
|---|---|---|
| `stub` | источника нет | **форма ответа**: «⟨N⟩ % к сроку · ожидаем: PropTech». Цифры и цитаты запрещены |
| `draft` | есть источник: стратегия, текущий сайт | настоящее значение + бейдж «стратегия, с. 47 · не согласовано» |
| `approved` | подтверждено человеком | `source`, `approvedBy`, `approvedAt` заполнены |

Поля: `status · owner · due · expectedForm · source · approvedBy · approvedAt`.
`expectedForm` обязателен при `stub` — иначе заглушка снова превращается
в бесполезное «нужны данные».

**Три уровня.** В данных — статус. На стенде — пунктир и бейдж «ждёт: `owner`»,
генерируемый из поля, а не из вёрстки. В проде — `check-stubs.js` роняет сборку.
Срок годности: старше `due` — жёлтая, старше `due` + 14 дней — красная и на стенде.

**Реестр.** `check-stubs.js` генерирует `docs/ЗАГЛУШКИ.md`: адрес, блок, статус,
чего ждём, кто снимает, до какого числа. Файл заменяет список открытых вопросов:
владельцу проще ткнуть в блок на живой странице, чем ответить на абстрактный вопрос.

🔴 **Границы.** Логотипы известных клиентов заводим сразу — реальные, с флагом
`logoApproved`; в прод без флага не уедет ни один. Отзыв — **каркасом**: тема,
готовая формулировка запроса клиенту, роль автора, `requestStatus`, **пустая цитата**.
Придуманный текст цитаты запрещён так же, как выдуманное число. Адреса
инфраструктуры — только `sitePrecision: district`. Обоснование — К-14 и К-16.

---

## 3. Фазы

Каждой — **Артефакт · Метрика · Инвариант (класс) · Откат**.
Фаза закрыта, когда метрика зелёная и мутация поймана. Не раньше.

**Закрытие фазы.** Метрика ниже — замысел. При закрытии **сюда же, к фазе**,
дописывается блок с фактом:

```
—— ЗАКРЫТА ДД.ММ · <коммит> ——
Метрика по факту: <число, а не «зелёный»>
Чем измерено:     <команда, которую можно повторить>
Где артефакт:     <файл, роут, экран>
🔴 Что НЕ доказано: <обязательное поле; «всё доказано» — недопустимый ответ>
```

Отсутствие блока у закрытой фазы и отсутствие строки **Откат** ловит
`check-memory.sh` §9. Фаза без отката считается необратимой и обязана
останавливать конвейер на приёмку владельцем.

---

### Ф0 · Предохранители

Репозиторий, в котором нельзя молча сломать. Единственная фаза без пользы
для посетителя — и единственная, без которой остальные семь недоказуемы.

**Артефакт.** Закрытые пишущие роуты · `npm test` во фронте · геоданные, схемы
и прототипы в репозитории · первая миграция БД · счётчик аналитики ·
`visual-compare` с эталоном «React до → React после» · ветка `redesign` ·
механизм заглушек §2.2: поля, `check-stubs.js`, реестр.

**Метрика.**

| Что | Сейчас | После |
|---|---|---|
| `curl -X DELETE /api/pages/delete-all` | **200** | **403** |
| `npm test` | команды нет | есть, красный на подсаженной ошибке |
| `find moscow.json` в репо | **0** | **1** |
| разворот БД с нуля | невозможен | схема совпадает с текущей |
| `grep -E "ym\(|gtag\("` по фронту | **0** | **> 0** |
| прод-сборка при не-`approved` блоке | проходит | **падает со списком адресов** |

**Инвариант.** Ни одна мера не считается внедрённой без пойманного искусственного
инцидента. Сторожит **К-05** (формальный признак принят за результат)
и **К-09** (ложное срабатывание собственного аудита).

**Откат.** Все меры аддитивны: роуты закрываются флагом `auth`, схемы лежат отдельной
папкой, скрипты добавляются. `git revert` одного коммита возвращает состояние до фазы.

Вводных не требует. ~3 сессии.

—— ЗАКРЫТА 19.08 · 06a08ff ——
Метрика по факту:
  анонимный DELETE /pages/delete-all .......... 403  (было 200)
  npm test .................................... 32 теста, 4 файла, зелёные
  moscow.json в репозитории ................... 1  (было 0)
  файлов с вызовом ym( во фронте .............. 1  (было 0)
  схема с нуля ................................ 158 = 158
  ворота заглушек на не-approved блоке ........ код возврата 1
  страниц в базе после всех мутаций ........... 216 (не пострадали)
Чем измерено:
  curl -X DELETE http://localhost:1337/api/pages/delete-all
  cd mgts-frontend && npm test
  git ls-files design/geo/moscow.json | wc -l
  grep -rlE 'ym\(' mgts-frontend/src | wc -l
  node mgts-backend/scripts/check-fresh-schema.mjs
  node mgts-frontend/scripts/check-stubs.mts --gate   (на фикстуре)
Где артефакт:
  9 PR смержено: #1…#9. Ветки step/* удалены.
  mgts-backend/src/api/page/routes/custom-page.ts — 6 роутов закрыты
  mgts-frontend/tests/ — 4 файла тестов
  mgts-frontend/src/lib/stubs.ts + scripts/check-stubs.mts — механизм заглушек
  mgts-frontend/src/lib/analytics.ts + components/analytics/ — счётчик
  mgts-backend/scripts/check-fresh-schema.mjs — воспроизводимость
  mgts-frontend/scripts/visual-baseline.mjs — эталон «React до → после»
  design/geo/, design/prototypes/, docs/redesign/ — артефакты редизайна
🔴 Что НЕ доказано:
  1. Из 21 замка §9 проверено мутацией 5. Остальные 16 относятся к фазам,
     которые ещё не начинались.
  2. Ворота заглушек не подключены к сборке и проверены только на фикстуре:
     реальных размеченных данных нет, meta.stub переезжает в Ф2.
  3. Аналитика не имеет идентификатора счётчика и ни одного вызова со страниц:
     доказано, что событие уходит, а не что оно отправляется из нужного места.
  4. Авторизованный путь к закрытым роутам не проверен — единственный способ
     проверить delete-all по-настоящему это выполнить его.
  5. Контент не воспроизводится с нуля (B-34): схема собирается, страниц 0.
  6. Секреты вычищены из HEAD, но остаются в истории публичного репозитория.
  7. Визуальный эталон снят на 12 страницах из 100 — полный прогон это первый
     шаг Ф1, а не результат Ф0.

---

### Ф1 · Тема на 118 страницах

Первая фаза, которую видно глазами, и та, что показывается коммерческому директору
до единого нового компонента.

**Артефакт.** Рабочая сборка Tailwind · токены в `globals.css` · шрифты через
`next/font` · светлая и тёмная тема без `!important` · снимки 118 страниц до и после.

**Метрика.** Числа базового прогона Ф1.5 → цель:

| Показатель | Сейчас | Цель |
|---|---:|---:|
| Текстовых узлов ниже AA | 2470 | **0** |
| `!important` | 206 | **0** |
| Литеральных цветов вне токенов | 1143 | **≤ 100** |
| Различных кеглей | 53 | **≤ 10** |
| Различных теней | 50 | **≤ 6** |
| Правил `:focus-visible` | 0 | **≥ 1** |
| Страниц без `h1` | 13 | **0** |
| Страниц с ошибкой гидратации | 31 | **0** |
| Сдвигов геометрии против снимка «до» | — | **0 на 100 страницах** |

Утилит в CSS-бандле > 0 — закрыто в PR #10 (было 0).

**Дефекты реестра (§10):** Д-01 · Д-02 · Д-03 · Д-06 · Д-31 — закрыты.
🔴 Д-04 (шкала кегля) и Д-05 (тени) **переехали в Ф1.6**: смена кегля двигает
раскладку, а инвариант этой фазы её запрещает. Д-07 (заголовки) переехал в Ф3,
Д-08 (гидратация) — в Ф2: ни то, ни другое не про тему. Перенос сужает фазу,
и это сказано вслух: закрыт не весь список, а тот, что остался после переноса.

**Инвариант.** Тема не меняет раскладку: правится только визуальный слой.
Сторожит **К-02** (правишь источник — не прошёл по потребителям).

**Откат.** Тема живёт в токенах и одном слое CSS. `git revert` возвращает прежний вид;
контент и разметка не менялись — откатывать в CMS нечего.

Вводных не требует. ~2–3 сессии.

—— 🔴 ЗАКРЫТИЕ ОТОЗВАНО 19.08 ——
Фаза была объявлена закрытой и закрытой не является. Владелец открыл `about_mgts`
в светлой теме и увидел то, чего прибор не показал: заголовок героя стал тёмным
поверх тёмной фотографии. Регрессия внесена этой же фазой — до неё он был белым.

Почему прибор молчал: `design-metrics` берёт фон из вычисленных стилей и
пропускает узлы, под которыми картинка или градиент. Это записано в границах
честности отчёта и в дефекте Д-30 — и это ничего не меняет. **Слепое пятно,
записанное в документ, остаётся слепым пятном.** Метрика «ноль» относилась к
двум третям сайта, а закрывающий блок подавал её как «ноль».

Заведён недостающий прибор `scripts/contrast-over-media.mjs`: фон берётся из
пикселей снимка, а не из стилей. Замер на 25 страницах:
  поверх медиа, тёмная тема ..... 0 ниже AA   (на базовой линии 94 на 12 страницах)
  поверх медиа, светлая тема .... 162 ниже AA (на базовой линии 76 на 24 страницах)
Светлая тема поверх картинок стала ХУЖЕ, чем была. Фаза открыта.

—— замер на момент отзыва · `8bd5054` ——
Метрика по факту (100 опубликованных страниц, два прогона прибора):
  узлов текста ниже AA, тёмная тема .......... 0      (было 2470)
  узлов текста ниже AA, светлая тема ......... 6      (было 6717)
  пар «цвет/фон» ниже AA, тёмная ............. 0      (было 10)
  `!important` в исходниках .................. 0      (было 206)
  литеральных цветов вне токенов ............. 98     (было 382)
  правил `:focus-visible` .................... 2      (было 0)
  объявлений гарнитуры ....................... 1 через next/font (было 9 + CDN)
  геометрия: коробок сдвинулось .............. 1231 из 35 869 на 52 страницах
  геометрия: исчезло / появилось ............. 0 / 0
  тестов ..................................... 35 зелёных
Чем измерено:
  node scripts/design-metrics.mjs --out ../docs/design-review/metrics-after.json
  node scripts/design-metrics.mjs --theme light --out ../docs/design-review/metrics-after-light.json
  node scripts/geometry-baseline.mjs compare .geo/f1-before .geo/f1-after
  node scripts/check-defects.mjs · npm test
Где артефакт:
  design/tailwind/tokens.css — слой токенов, две темы
  design/tailwind/tailwind.config.cjs — утилиты через переменные
  mgts-frontend/src/app/light-theme.css — было 689 строк, стало 0 правил
  mgts-frontend/scripts/geometry-baseline.mjs — эталон геометрии
  docs/design-review/metrics-after*.json, defects.md
🔴 Что НЕ доказано:
  1. Геометрия всё-таки сдвинулась: 1231 коробка, максимум 35 px. Причина названа
     (`.font-display` рисуется самохостящимся Space Grotesk), но глазами
     ни одна из 52 страниц не просмотрена.
  2. Светлая тема не идеальна: 6 узлов на 2 страницах остались нечитаемы —
     легаси-блоки `design/html_blocks` красятся стоковыми утилитами (Д-38).
  3. Что именно рисует кириллицу — по-прежнему неизвестно (Д-34), и preflight
     перебивает задуманный шрифт (Д-35). Тема закрыта поверх этой неясности.
  4. Контраст поверх картинок и градиентов прибор пропускает: 5036 узлов из
     15 969. Ноль в отчёте — это ноль по видимой части, не по всей.
  5. Дефекты Д-04, Д-05, Д-07, Д-08 не исправлены, а перенесены в другие фазы.
  6. Ловушка Л-1 соблюдена побайтово, но девять литералов внутри
     `PageRenderer.tsx` так и остались (Д-37).
——

---

### Ф1.5 · Дизайн-ревью

Требование владельца 19.08. Претензия коммерческого директора — «сайт не выглядит
инновационно и дорого» — до сих пор не имеет ни одного числа. Ревью превращает её
в перечень дефектов, который можно закрывать и предъявлять.

**Почему здесь, а не в конце.** Ревью после сдачи — это отчёт о том, что уже поздно
менять. Ревью после темы (Ф1), но до новых компонентов (Ф2–Ф6) даёт чек-лист,
по которому эти компоненты строятся. Дороже всего исправлять то, что уже размножено
по семнадцати компонентам.

**Прогонов два.** Базовый — на текущем сайте, сразу; повторный — на стенде в Ф7.
Разница между ними и есть доказательство, что дизайн улучшился, а не «нам кажется».

**Артефакт.** Пять отчётов в `docs/design-review/`, каждый прогоняется навыком Claude Design:

| Отчёт | Навык | Что даёт |
|---|---|---|
| `critique.md` | `design:design-critique` | иерархия, читаемость, консистентность; дефекты с адресами страниц |
| `accessibility.md` | `design:accessibility-review` | WCAG 2.1 AA: контраст, клавиатура, размеры целей, скринридер |
| `design-system.md` | `design:design-system` | захардкоженные значения, разнобой имён, дубли токенов |
| `ux-copy.md` | `design:ux-copy` | подписи кнопок, ошибки, пустые состояния, тексты форм |
| `handoff.md` | `design:design-handoff` | спека на новые компоненты: состояния, брейкпоинты, краевые случаи |

**Метрика.** Числа, а не мнения. Прибор — `mgts-frontend/scripts/design-metrics.mjs`
(Playwright, реальные вычисленные стили). Базовый прогон снят **19.08 на 100
опубликованных страницах** в двух окнах:

| Что | Базовый прогон | Порог |
|---|---|---|
| Текстовых узлов ниже AA | **2470** из 10931 проверенных | **0** |
| Пар «цвет/фон» ниже AA | **10** | **0** |
| Литеральных цветов вне токенов | **1143** вхождения, 481 различное | ≤ **100** |
| `!important` | **206** (по всем стилям, не только теме) | **0** |
| Интерактивных целей меньше 44×44 px | **206** видов (мобильное окно — 179) | **0** |
| Страниц с боковой прокруткой, окно 390 | **100 из 100** (до 285 px) | **0** |
| Страниц с боковой прокруткой, окно 1366 | **64 из 100** | **0** |
| Страниц без `h1` / с несколькими `h1` | **13** / **7** | 0 / 0 |
| Иконок из CMS / аварийных | **7 / 7624** | один источник |
| Блоков текста-заглушки в проде | **17** на 4 страницах | 0 либо `stubStatus` |
| Страниц с пустым телом | **10 из 100** | 0 опубликованных |
| Страниц без фокусируемой навигации | **0** | 0 (не ухудшить) |
| Изображений без `alt` | **0** | 0 (не ухудшить) |

🔴 Прежняя строка «`!important` было 181» была замером одного файла; прибор
считает все стили проекта — **206**. Порог не изменился, изменился охват.

**Ворота.** `node scripts/design-metrics.mjs --gate` возвращает 1, пока пороги не
взяты. Сегодня красные по пяти строкам — это правильное состояние до Ф1.

**Инвариант.** Дефект дизайна закрывается правкой, а не переносом срока: у каждого
пункта в отчёте есть либо коммит, либо явная запись «отложено, потому что». Сторожит
**К-10** (обещание без носителя) и **К-05** (формальный признак принят за результат).

**Откат.** Отчёты — документы, ничего не исполняют. Правки по ним идут отдельными
шагами и откатываются каждая своим `git revert`.

—— БАЗОВЫЙ ПРОГОН СНЯТ 19.08 ——
Артефакты: `docs/design-review/` — пять отчётов, `metrics-before.json`,
`metrics-before-mobile.json`, `page-fill.json`, прибор
`mgts-frontend/scripts/design-metrics.mjs`.
🔒 Замки сняты 5 из 5: тёмный текст на тёмном (6→10 пар), навигация без ссылок
(0→5 страниц), цели 12×12 (65→130), полоса 5000 px (100→3634 px), ворота (1↔0).
M2 в первой редакции дал ложный зелёный — прибор считал фокусируемым поле поиска;
это **К-04**, исправлено и переснято.
🔴 Что НЕ доказано на этом шаге:
  1. что отчёты приведут к правкам — ни один дефект ещё не закрыт;
  2. контраст текста поверх картинок и градиентов — 5038 узлов из 15969 (32 %)
     прибор пропускает молча (К-04, не закрыт);
  3. скринридером и зумом 200 % не проверялось ничего;
  4. светлая тема не измерялась ни разу — все числа сняты на тёмной;
  5. замеры сняты на dev-сервере Next.js, прод-сборка может отличаться;
  6. 18 страниц из 118 в замер не попали: в базе опубликовано 100.
——

🔴 **Чего ревью не заменяет.** Оно не отвечает на вопрос «нравится ли коммерческому
директору». Приёмка человеком остаётся в Ф7; ревью лишь снимает с неё всё, что
можно померить машиной, чтобы разговор шёл про замысел, а не про контраст кнопки.

~2 сессии базовый прогон, ~1 повторный.

---

### Ф1.6 · Адаптив и попадание пальцем

Фазы не было в плане. Она заведена **19.08 по итогам Ф1.5**, потому что ревью
нашло то, чего план не предполагал: **мобильной вёрстки у сайта нет вообще**.
Не «плохо адаптируется» — документ шириной 668 px в окне 390 на **всех 100
страницах**. Служебная строка шапки не сворачивается, бургер-меню отсутствует.

Почему отдельной фазой, а не пунктом Ф1. Тема — это цвет и шрифт, она обещает
не трогать раскладку (инвариант Ф1, класс К-02). Адаптив раскладку меняет
намеренно. Складывать их в одну фазу — значит сделать инвариант Ф1 невыполнимым
и потерять оба замера.

**Артефакт.** Мобильное состояние шапки (свёрнутая служебная строка, бургер,
поиск иконкой) · обрезка декоративного слоя `light-leak` контейнером ·
высота попадания ≥ 44 px у служебных ссылок · ссылка «Перейти к содержимому» ·
доступные имена у полей поиска.

**Метрика.** Страниц с боковой прокруткой в окне 390 — **0 из 100** (сейчас 100).
В окне 1366 — **0** (сейчас 64). Видов целей меньше 44×44 — **0** (сейчас 206).
Узлов, пропущенных прибором контраста, — **≤ 1000** (сейчас 5038).

**Дефекты реестра (§10):** Д-09 · Д-10 · Д-11 · Д-12 · Д-13 · Д-30, плюс
переехавшие сюда после Ф1: Д-04 (шкала кегля), Д-05 (тени), Д-33 (`custom.css`
никогда не подключался), Д-34 (чем рисуется кириллица), Д-35 (preflight
перебивает шрифт), Д-38 (легаси-блоки в светлой теме). Одиннадцать строк —
это самая нагруженная фаза плана, и загрузили её приборы, а не отчёты.

**Инвариант.** Адаптив не меняет содержание: те же блоки, другой порядок и размер.
Сторожит **К-13** (отрицательный результат по одному пути принят за отсутствие
явления) — сайт проверялся только в одном окне, и это дало «всё в порядке».

**Откат.** Правки локализованы в компоненте шапки и одном контейнере-обёртке.
`git revert` возвращает прежнюю раскладку; контент и CMS не задеты.

Вводных не требует. ~1–2 сессии.

---

### Ф2 · Каркас CMS

**Артефакт.** 17 компонентов и 3 типа перенесены из `design/strapi-new/`
в `mgts-backend` · 9 секционных компонентов в Dynamic Zone · ветки
в `SectionRenderer` · тест «DZ = свитч» · webhook → `revalidateTag`.

**Метрика.** DZ **40 = свитч 40**. Правка в админке видна **сразу**, а не через 300 с.
Компонентов без вложенного `meta.stub` — **0**.

**Дефекты реестра (§10):** Д-14 · Д-15 · Д-16 · Д-17 · Д-18 · Д-19.

**Инвариант.** Неизвестный компонент не исчезает молча.
Сторожит **К-04** (молчание принято за исправность) и **К-11** (дубль вместо правки).

**Откат.** Компоненты добавляются, существующие не трогаются, DZ расширяется —
обратно удалить категории и ветки. 🔴 Необратим только снос 68 дублей (B-11):
перед ним обязателен дамп схемы, **проверенный восстановлением**. Это единственное
место фазы, останавливающее конвейер на приёмку.

Вводных не требует. ~3–4 сессии.

---

### Ф3 · Новая главная

Тот самый экран: развилка, карта Москвы подложкой, продуктовая лестница.

**Артефакт.** Главная из компонентов Ф2 · `api::geo` с геометрией · карта через
`next/dynamic` с `ssr:false`.

**Заглушки вместо ожидания:**

| Чего нет | Что ставлю | Статус |
|---|---|---|
| Параметры вида карты | масштаб 44, X 70, прозрачность 62, маска 30 | `draft` |
| Контент главной (Q-R01.2) | тексты из стратегии 2027–2029 | `draft` |
| Справочник районов (Q-R01.3) | OSM, 125 районов, `source: "OSM/ODbL"` | `draft` |
| Цифры клиентской выгоды | что есть в стратегии — числом с адресом страницы; чего нет — формой ответа | `draft` / `stub` |

**Метрика.** На страницах **без** карты запроса за геометрией **нет**.
Геометрия ≤ **100 КБ** gzip отдельным запросом. Первый кадр ≤ **150 мс**,
≥ **50 fps** на ноутбуке без внешней видеокарты. LCP главной не хуже текущего.
Показателей с `audience: company` на публичной странице — **0**.

**Дефекты реестра (§10):** Д-20 · Д-21 · Д-22 · Д-23 · Д-27 · Д-28.

**Инвариант.** Число на странице говорит о выгоде клиента, а не о нашей выручке.
Сторожит **К-14** (свой показатель выдан за пользу клиента) и **К-07**
(дефолт-значение принято за истинное).

**Откат.** Новая главная — отдельная страница в CMS; прежняя не удаляется, а снимается
с публикации. Возврат — переключение публикации, без кода.

~3–4 сессии.

---

### Ф4 · Продукты InfraTech

Самое денежное направление, которого на сайте нет: три страницы дают **86 % прироста**
в агрессивном сценарии стратегии.

**Артефакт.** Страницы: colocation в мини-ЦОД · тёмная оптика · BigTech и ЦОД.

**Заглушки вместо ожидания:** структура и разделы — из стратегии и раздела операторов,
статус `draft`. Параметры — **формой ответа**:

```
Colocation в мини-ЦОД
  ⟨N⟩ кВт на стойку             [ожидаем: InfraTech]
  ⟨N⟩ % SLA доступности         [ожидаем: InfraTech]
  ⟨N⟩ раб. дней до включения    [ожидаем: InfraTech]
  ⟨N⟩ площадок в пределах МКАД  [ожидаем: InfraTech]
```

Владелец направления видит не «заполните страницу», а список из четырёх чисел
с единицами измерения — разница между задачей на день и задачей на квартал.

**Метрика.** Каждая страница отвечает **200**, находится внутренним поиском, форма
ведёт в CRM с полным набором полей. Чисел со статусом `approved` и пустым `source` — **0**.
Полей со статусом `stub` без `expectedForm` — **0**. Цифр в полях `stub` — **0**.

**Дефекты реестра (§10):** Д-24 · Д-25.

**Инвариант.** Правдоподобное число без источника физически невозможно.
Сторожит **К-07** и **К-14**.

**Откат.** Три новые страницы, существующее не задето. Снять с публикации.

~2–3 сессии.

---

### Ф5 · Кейсы и карта кейсов

🔴 **Ответ владельца по Q-R01.7:** съёмок сейчас нет, но фото **приедет** — и при этом
по части объектов его не будет **никогда**. Карточка обязана уметь обе ситуации
и в обеих выглядеть замыслом, а не нехваткой.

**Три носителя, один слот.** Заполняется по приоритету тем, что есть:
фото до/монтаж/после → три состояния 3D-модели этажа → схема объекта плюс числовая
композиция. Раскладка карточки от носителя **не меняется**, иначе кейсы разъедутся
на «богатые» и «бедные», и вторые прочтутся как брак.

**Два разных «нет».** `mediaExpected: true` — съёмка запланирована, кейс в реестре
заглушек со сроком. `mediaExpected: false` — фото не будет никогда, это финальное
состояние, из реестра исчезает. Без различия объекты без съёмки вечно желтели бы
в отчёте и обесценили его.

**Артефакт.** `api::case` · карта кейсов с фильтрами · карточка с тремя носителями ·
числовые факты, тип объекта, объём работ, сроки.

**Заглушки вместо ожидания:** три демо-кейса на реальных типах работ (жилой комплекс,
бизнес-центр, объект городской инфраструктуры), статус `stub`, факты формой ответа:
`⟨N⟩ м трассы`, `⟨N⟩ этажей`, `⟨N⟩ раб. дней`. Стоковые фотографии не ставим ни в каком
виде: сток читается как настоящий объект. Логотипы известных клиентов заводим в
`api::client` сразу. Отзыв — каркасом: клиент, роль автора, тема, формулировка запроса,
`requestStatus`, пустая цитата. Название объекта обезличено («ЖК на северо-западе»).

**Метрика.** У всех опубликованных кейсов `quoteApproved` и `clientLogoApproved` —
**true**. `sitePrecision` по умолчанию `district`: точных адресов инфраструктуры
на публичной карте **нет ни одного**. Высота и сетка карточки одинаковы
на всех трёх носителях — расхождение геометрии **0** на трёх контрольных кейсах.

**Инвариант.** Ничего непроверенного о клиенте не публикуется, и отсутствие фото
не деформирует раскладку. Сторожит **К-16** (прототип принят за требования)
и **К-04** (молчание принято за исправность).

**Откат.** `api::case` и карта кейсов автономны: блок снимается со страницы,
тип контента остаётся с данными. Фотослот аддитивен.

Зависимость: **B-29** — вердикт по 3D-рендеру до старта. ~3–4 сессии.

---

### Ф6 · Живые действия

Сайт перестаёт быть витриной и начинает принимать обращения.

**Артефакт.** Формы подключены (B-05) · вход в личный кабинет на открытой
поверхности · 13 страниц услуг связи МТС · события аналитики на развилке и формах.

**Заглушки вместо ожидания:** адресата лидов нет — лид сохраняется в `api::lead`
и пишется в лог, внешняя отправка выключена флагом `LEAD_FORWARD=off`. Форма при этом
полностью рабочая, меняется одна переменная окружения. Q-R01.1 решён мягко и обратимо:
13 страниц МТС остаются, но получают баннер-форвард на МТС Бизнес; снятие — после
первых данных счётчика, а не до.

**Метрика.** Форм с `action="#"` — **0**. E2E отправляет форму, бэкенд получил все поля.
По каждой ветке развилки в счётчике есть событие.

**Дефекты реестра (§10):** Д-26 · Д-29.

**Инвариант.** Действие посетителя доходит до адресата или явно не отправляется —
третьего состояния нет. Сторожит **К-04** и **К-05**.

**Откат.** Отправка лидов за флагом `LEAD_FORWARD`, баннер-форвард — блок в CMS.
🔴 Необратимо снятие 13 страниц МТС — именно поэтому они не снимаются, а получают
баннер, до данных счётчика.

~2–3 сессии.

---

### Ф7 · Стенд и приёмка

**Артефакт.** Новая версия сайта на превью-стенде · регрессия по 118 страницам ·
`docs/ЗАГЛУШКИ.md` как повестка приёмки · отчёт по пяти критериям §1.

**Заглушка вместо ожидания (Q-R01.6):** стенд поднимаю сам — превью из ветки
`redesign`, закрытое паролем и `noindex`. Требования ИБ к внешнему хостингу снимут
эту заглушку.

**Метрика.** Страниц с 404 или 500 — **0**. Сдвигов раскладки против эталона Ф1 — **0**.
Все замки §9 сработали на мутациях. К1–К5 из §1 зелёные.

**Дефекты реестра (§10):** Д-32 + повторный прогон прибора и разница с базовым.

**Инвариант.** Приёмка идёт по числам §1, а не по впечатлению; впечатление
коммерческого директора фиксируется отдельно и правит Ф1, а не конец плана.
Сторожит **К-05** (формальный признак принят за результат).

**Откат.** Стенд отдельный, прод не затронут. Снести стенд — одна команда.

~2 сессии.

---

## 4. Риски и уроки

### 4.1 🔴 Четыре ловушки

**Л-1 · `PageRenderer.tsx` — не трогать.** Каскад `if` по 15 шаблонам на 739 строк
с недостижимыми ветками (`:517-539` после `return` на `:256`). Новые секции — только
через `SectionRenderer`, он плоский `switch`. Проверка: `git diff` не содержит правок
этого файла.

**Л-2 · `SectionRenderer.tsx:143` — `default: return null`.** Неизвестный компонент
исчезает молча. Добавляем 11 новых — первая опечатка даст «в CMS есть, на странице нет,
ничего не падает». Класс **К-04**.

**Л-3 · Доставка.** 80 КБ геометрии не должны попасть в бандл — отдельный запрос
и `next/dynamic` с `ssr:false`. Плюс `revalidateTag` не вызывается **нигде**:
правка в админке доедет через 300 с. Класс **К-02**.

**Л-4 · Заглушка тихо становится фактом.** Придуманный отзыв или выдуманная цифра
выглядят на странице ровно как настоящие. Классы **К-14** и **К-16** — оба уже
сработали на этом проекте. Механизм §2.2 делает подделку невозможной, а не
нежелательной.

### 4.2 Уроки этой сессии

| Что случилось | Класс | Что изменилось в плане |
|---|---|---|
| Объявил push невозможным, проверив 2 канала из 3 | **К-13** | среда агента описана в `topics/agent-environment.md` |
| Метрики о нашей выручке выданы за клиентскую выгоду | **К-14** | поле `audience` в `data.metric`, инвариант Ф3 |
| Рисовал карту Москвы процедурно вместо взятия данных | **К-15** | география только из данных, `topics/moscow-map.md` |
| Прототип выдан за требования | **К-16** | §2.2 и требования отдельным документом |
| Восемь ходов работы без записи в память | **К-10** | `check-memory.sh` §8 |
| Первая версия проверки планов дала ложный зелёный | **К-09** | мутация обязательна для самой проверки |

🔴 **Из 16 классов каталога машину имеют три** — `patterns/coverage.md`.
Семь назначены этим планом и поднимутся до ✅ только после мутации.

---

## 5. Открытые вопросы

Большинство переведено в заглушки: отвечать в переписке не нужно, достаточно
посмотреть стенд и снять пометку. Здесь они остаются как след решения.

### Решения (закрыты)

| Код | Вопрос | Решение |
|---|---|---|
| Q-R01.1 | Судьба 13 страниц услуг связи МТС | остаются с баннером-форвардом; снятие после счётчика |
| Q-R01.2 | Кто заводит контент главной | пишу сам из стратегии, статус `draft` |
| Q-R01.3 | Справочник районов | OSM, 125 районов |
| Q-R01.4 | Кейсы модалкой или страницами | отдельные страницы с URL: SEO дороже, откат дешевле |
| Q-R01.5 | Куда класть прототипы и геоданные | `design/`; закрывается в Ф0 |
| Q-R01.6 | Внешний хостинг стенда | своё превью под паролем и `noindex` |
| Q-R01.7 | Фото объектов «до / монтаж / после» | *владелец 17.08:* сейчас нет, приедет, по части объектов не будет никогда → три носителя и `mediaExpected` |
| Q-R01.8 | Кто ставит `approved` | *владелец 17.08:* владельцы направлений в коммерческом блоке, ролью |

### Открыт

**Q-R01.9 · В каком состоянии 3D-рендер этажей из соседней ветки?**
Какие форматы моделей принимает, есть ли модели хотя бы по одному объекту, вес
и производительность в браузере. После ответа по Q-R01.7 он перестал быть украшением
и стал несущей конструкцией Ф5. **Рекомендация: замерить до старта Ф5** — это разведка,
а не фаза (принцип 9 стандарта). Задача **B-29**.

Плюс 9 вопросов из требований к карте и 5 из плана скоринга — в доставленных
документах, все закрыты заглушками.

---

## 6. Артефакты и метрики по фазам

| Фаза | Артефакт | Ключевая метрика | Инвариант (класс) | Обратима |
|---|---|---|---|---|
| Ф0 | предохранители, механизм заглушек | 403 вместо 200; сборка падает на заглушке | К-05, К-09 | ✅ |
| Ф1 | тема на 118 страницах | 0 сдвигов геометрии | К-02 | ✅ |
| Ф2 | 17 компонентов, тест DZ | DZ 40 = свитч 40 | К-04, К-11 | 🔴 частично: B-11 необратим |
| Ф3 | новая главная и карта | 0 запросов геометрии вне карты | К-14, К-07 | ✅ |
| Ф4 | 3 страницы InfraTech | 0 чисел без источника | К-07, К-14 | ✅ |
| Ф5 | кейсы, карта кейсов | 0 расхождений геометрии на 3 носителях | К-16, К-04 | ✅ |
| Ф6 | формы, кабинет, аналитика | 0 форм с `action="#"` | К-04, К-05 | 🔴 частично: снятие 13 страниц |
| Ф7 | стенд и приёмка | 0 страниц с 404/500 | К-05 | ✅ |

## 7. Реестр заглушек: кто снимает

Ни одна фаза этого не ждёт. Но **в прод не уедет ничего** из списка, пока статус
не станет `approved`.

| Заглушка | Фаза | Статус | Кто снимает | Цена промедления |
|---|---|---|---|---|
| Параметры вида карты | Ф3 | `draft` | владелец | косметика, минуты |
| Тексты главной | Ф3 | `draft` | владельцы направлений | правки на стенде |
| Справочник районов | Ф3 | `draft` | владелец ГИС | точки кейсов сместятся |
| Цифры клиентской выгоды | Ф3 | `stub` | коммерческий блок | 🔴 главная без доказательств |
| Параметры 3 продуктов | Ф4 | `stub` | владельцы направлений | 🔴 денежные страницы пустые |
| Кейсы: названия, отзывы, логотипы | Ф5 | `stub` | PropTech | карточки обезличены |
| Адресат лидов | Ф6 | флаг `off` | владелец | лиды копятся, не уходят |
| Внешний хостинг стенда | Ф7 | своё превью | владелец / ИБ | — |

**Владелец заглушек — роль**, «владелец направления, коммерческий блок»
(решение 17.08). ⚠️ У роли нет почты: отчёт `docs/ЗАГЛУШКИ.md` кладётся
в репозиторий и на стенд, а не рассылается. Появится имя — добавится адрес.

🔴 **Самая дорогая заглушка — цифры.** Уязвимое место не карточка кейса, а главная:
формы ответа вместо клиентской выгоды. Сайт может выглядеть дорого и ничего
не доказывать, а «инновационно и дорого» — ровно про доказательства.

---

## 8. DoD

**Фаза Done** = артефакт есть + все метрики фазы зелёные + инвариант держится
(мутация поймана) + закрывающий блок дописан к фазе.

**План Done** = все восемь фаз Done + замок готовности не нарушен + вопросы §5 закрыты
или сознательно оставлены заглушками с назначенным владельцем.

---

## 9. Замок готовности

🔒 **Мера не считается внедрённой, пока проверка не поймала искусственный инцидент.**

Нарушен — ничего не выкатываем. Ни одна строка ниже пока не проверена: в табло §0.0
это **0 ✅ из 21**.

| # | Фаза | Мутация | Проверка обязана |
|---:|---|---|---|
| 1 | Ф0 | анонимный `DELETE /pages/delete-all` | вернуть 403 |
| 2 | Ф0 | подсаженная ошибка в тесте | `npm test` красный |
| 3 | Ф0 | не-`approved` блок в прод-сборке | сборка падает со списком адресов |
| 4 | Ф0 | цифра в поле со статусом `stub` | проверка красная |
| 5 | Ф0 | текст цитаты в `quote` при статусе `stub` | проверка красная |
| 6 | Ф0 | `stub` без `expectedForm` | проверка красная |
| 7 | Ф0 | `approved` без `source` | проверка красная |
| 8 | Ф0 | заглушка старше `due` + 14 дней | красная на стенде |
| 9 | Ф0 | убрать бейдж заглушки из вёрстки | тест красный: бейдж генерируется из поля |
| 10 | Ф1 | убрать токен из `globals.css` | визуальный тест красный |
| 11 | Ф1 | правка `PageRenderer.tsx` | проверка Л-1 красная |
| 12 | Ф2 | фиктивный компонент в схему | тест «DZ = свитч» красный |
| 13 | Ф2 | отключить webhook | правка не доезжает, ловится |
| 14 | Ф2 | компонент без вложенного `meta.stub` | проверка красная |
| 15 | Ф3 | втащить геометрию в бандл | проверка веса красная |
| 16 | Ф3 | показатель с `audience: company` на публичной странице | проверка красная |
| 17 | Ф5 | кейс с `quoteApproved: false` | в прод не собирается, на стенде с бейджем |
| 18 | Ф5 | логотип без `logoApproved` в прод-сборке | сборка падает |
| 19 | Ф5 | нет фото при `mediaExpected: false` | раскладка не меняется, в реестр не попадает |
| 20 | Ф5 | нет фото при `mediaExpected: true` | попадает в реестр со сроком |
| 21 | Ф6 | сломать поле формы | e2e красный |
| 22 | Ф1.5 | `h1,h2,h3` тёмным по тёмному | пар ниже AA больше ✅ 6 → 10 |
| 23 | Ф1.5 | спрятать все ссылки шапки и меню | страниц без фокусируемой навигации больше ✅ 0 → 5 |
| 24 | Ф1.5 | ссылки подвала 12×12 px | целей меньше 44×44 больше ✅ 65 → 130 |
| 25 | Ф1.5 | `body::after` шириной 5000 px | переполнение по горизонтали растёт ✅ 100 → 3634 px |
| 26 | Ф1.5 | поднять пороги ворот до недостижимых | код возврата `--gate` 1 → 0 ✅ |
| 27 | Ф1.5 | дефект помечен ✅ при отсутствующем `metrics-after.json` | красная: «закрывать нечем» ✅ |
| 28 | Ф1.5 | дефект помечен ✅, а число не взято | красная с числом и условием ✅ |
| 29 | Ф1.5 | ручной дефект закрыт словом «сделано» | красная: нет `коммит:<sha>` ✅ |
| 30 | Ф1.5 | ручной дефект закрыт несуществующим sha | красная: коммита нет в истории ✅ |
| 31 | Ф1.5 | дефект отложен без «потому что» | красная ✅ |
| 32 | Ф1.5 | у дефекта стёрта колонка «Фаза» | красная ✅ |
| 33 | Ф1.5 | поле замера переименовано / отсутствует | красная: «поля нет в замере» ✅ |
| 34 | Ф1 | два снимка одного и того же состояния | эталон геометрии обязан дать 0 расхождений ✅ |
| 35 | Ф1 | `.site-main { padding-left: 10px }` | эталон геометрии обязан покраснеть ✅ 707 коробок, 5 страниц из 5 |

⚠️ Замки 22–33 сняты 19.08. Две проверки при этом дали **ложный результат** и были
переписаны: прибор считал фокусируемой навигацией поле поиска в шапке (К-04), а
`check-defects` красил все 18 ручных дефектов, потому что `\b` в JavaScript не
видит границы после кириллицы. Обе ошибки поймал запуск, ни одну — чтение.

⚠️ **Проверку тоже мутировать.** 17.08 первая версия секции 9 `check-memory.sh`
рапортовала «✓ у всех 8 фаз назван откат» при нуле откатов: `grep -c` при нуле
совпадений ломал сравнение, и ветка уходила в «всё хорошо». Это **К-09**, и поймано
запуском, а не чтением. Ни одна строка таблицы выше не считается закрытой, пока
мутация не воспроизведена.

---

## 10. Реестр дефектов дизайна

Полный реестр — `docs/design-review/defects.md`. Здесь только правило и сводка.

**Правило.** Дефект, найденный в Ф1.5, закрывается ровно одним из двух способов:

1. **числом** — прибор `design-metrics` показывает, что условие выполнено;
2. **коммитом** — для дефектов без прибора статус обязан содержать `коммит:<sha>`,
   и sha обязан существовать в истории.

Третьего способа нет. «Учтём», «сделано в рамках общей правки», «выглядит лучше»
машина не принимает — это **К-10**, обещание без носителя.

Отложить можно, но только с причиной: статус `⏸ отложено, потому что …`.

**Чем проверяется:** `node mgts-frontend/scripts/check-defects.mjs` — код возврата 1,
если хоть одна строка нарушает правило. 🔒 Мутации 27–33 §9.

| Фаза | Дефектов | Из них с прибором |
|---|---:|---:|
| Ф1 · тема | 9 | 8 |
| Ф1.6 · адаптив | 6 | 4 |
| Ф2 · компоненты | 6 | 1 |
| Ф3 · главная и содержание | 6 | 1 |
| Ф4 · продукты | 2 | 0 |
| Ф6 · живые действия | 2 | 0 |
| Ф7 · приёмка | 1 | 0 |
| **Всего** | **32** | **14** |

🔴 **Восемнадцать дефектов из тридцати двух прибора не имеют.** Это не повод не
заводить их — это повод не называть ревью «пройденным», когда зелёными станут
только четырнадцать.
