# Покрытие классов ошибок машинным наблюдением

Дата: 17.08.2026 | Статус: в работе | Отменяет: —

На этот файл ссылается чек-лист метрики в `../PLANNING-STANDARD.md` §3.
Смысл: прежде чем написать «инвариант сторожит К-NN», проверь, есть ли у К-NN
вообще хоть одна машина. Если стоит ❌ — **эта фаза и есть повод её завести**,
а не повод сослаться и забыть.

**Правило колонки «чем наблюдается»:** сюда пишется команда, скрипт или тест —
то, что можно запустить. «Внимательность», «ревью» и «помним» наблюдением
не являются.

| Класс | Кратко | Чем наблюдается | Статус |
|---|---|---|---|
| К-01 | проблему показали на примере — она шире примера | — | ❌ |
| К-02 | правишь источник — не прошёл по потребителям | тест «DZ = свитч» (Ф2, планируется) | 🟡 |
| К-03 | прибор смотрит мимо | `visual-compare` с эталоном «React до → после» (Ф0) | 🟡 |
| К-04 | молчание принято за исправность | `SectionRenderer` `default:` → лог вместо `null` (Ф2) | 🟡 |
| К-05 | формальный признак принят за результат | закрывающий блок с полем «что НЕ доказано» (стандарт §1.12) | 🟡 |
| К-06 | конфигурация принята за состояние | — | ❌ |
| К-07 | дефолт-значение принято за истинное | `check-stubs.js`: цифра при `stub` → красный (Ф0) | 🟡 |
| К-08 | источник истины отстал от реальности | `check-memory.sh` §8: память не старше правок проекта | ✅ |
| К-09 | ложное срабатывание собственного аудита | мутационный тест каждой проверки (замок готовности) | ✅ |
| К-10 | обещание без носителя | `check-memory.sh` §7 ритуалы + §8 грязное дерево | ✅ |
| К-11 | дубль вместо правки | `grep` на вторую реализацию; B-11 (68 дублей схем) | 🟡 |
| К-12 | одноразовая машина оставлена в проекте | — | ❌ |
| К-13 | отрицательный результат по одному пути принят за отсутствие явления | — | ❌ |
| К-14 | свой показатель выдан за пользу клиента | `data.metric.audience: company` на публичной странице → красный (Ф3) | 🟡 |
| К-15 | рисую от руки то, что есть в данных | — | ❌ |
| К-16 | прототип принят за требования | инвариант Ф5: непроверенное о клиенте не публикуется, `quoteApproved`/`logoApproved` (Ф5) | 🟡 |

**Счётчики:** ✅ работает 3 · 🟡 машина назначена, но не внедрена 8 · ❌ нет 5.
Сумма 16 = число классов в каталоге.

---

## Что из этого следует

🔴 **Из 16 классов машину имеют три.** Остальное держится на том, что владелец
поймает — а он и ловил: К-14, К-15 и К-16 все три открыты им, не мной.

🟡 **Семь машин уже назначены** внутри PLAN-01 и появятся вместе со спринтами Ф0–Ф3.
Их статус поднимется до ✅ **только после мутации**, не после написания кода.

❌ **Пять классов наблюдать сегодня нечем.** Из них К-15 сработал на этом проекте
буквально вчера. К-16 после оформления PLAN-01 по стандарту получил инвариант в Ф5.
Идеи дешёвых проверок для оставшихся:

- **К-15** (рисую от руки то, что есть в данных): перед тем как генерировать
  геометрию, координаты или справочник процедурно — `grep` по репозиторию
  и три минуты поиска открытого датасета. Формализуется как пункт чек-листа
  перед фазой, где появляется новый пространственный или справочный слой.
- **К-16** (прототип принят за требования): у прототипа в репозитории обязан лежать
  рядом файл требований либо явная пометка «требований нет». Проверяется обходом
  `design/prototypes/`.

Заводить их отдельной задачей не буду — по правилу «отдельной задачи на дисциплину
не существует» (К-10). Они войдут в **B-31** машинной проверкой планов и артефактов.
