# Контекст слота `arch`

Ветка: `main` · Машина: macbook-air-andrey-efremov-local · Лимит файла: **120 строк**

## Текущая задача (с 17.08)
**Редизайн сайта.** Концепция принята после 7 итераций, прототипы и планы сделаны,
в репозиторий ничего не положено. Табло — `implementation-plans/PLAN-01`, §0.0.
Следующий шаг — B-04 (сборка Tailwind), она предусловие ко всему.

⚠️ git по этому репозиторию — через **Desktop Commander**, не через `device_bash`
(`topics/agent-environment.md`). На машине бывает залипший `.git/index.lock` от соседней
сессии — снимать `rm`, Desktop Commander это умеет.

## Текущая задача
Заведён мемори-банк по `МЕТОДОЛОГИЯ-работы-с-ИИ-агентами.md`, проведён полный аудит
проекта. Знания разложены в `topics/`, находки — в `backlog.md` и `inbox/2026-08.md`.

## Что сделано в сессии 2026-08-14
1. Прочитана методология, спроектирована структура `memory-bank/`.
2. Репозиторий склонирован и исследован четырьмя параллельными агентами:
   фронт, бэк, `docs/` (210 файлов), пайплайн + легаси.
3. Старый «мемори-банк» найден: отдельной папки не было, его роль играли
   `docs/project/CONTEXT.md`, `PROJECT_CONTEXT_RESTORED.md` и `docs/status/**`.
   Разложен в новую конструкцию, поколение A вынесено в `_to_delete/`.
4. `check-memory.sh` написан и проверен мутацией (см. `sessions/2026-08/`).

## Как здесь принято работать (замеры, а не память)
- Любое утверждение о состоянии проекта требует артефакта: путь + строка,
  либо вывод команды. «Я посмотрел, там всё в порядке» результатом не считается.
- Перед правкой открывать `patterns/catalog.md` по колонке «Триггер».
- Даты в `docs/**` не совпадают с git: репозиторий залит сквошем
  `349101c` (2026-03-04), хронология восстанавливается только по текстам документов.

## Локальный запуск (проверено по коду, не прогоном)
```
./scripts/dev/start_all.sh      # статика design/ :8002 + Strapi :1337, pid/логи в .dev/
cd mgts-frontend && npm run dev # Next :3000
node scripts/qa/check-pages.js  # сверка Strapi ↔ Next
node mgts-frontend/scripts/visual-compare.js   # нужны все три сервера подняты
```
⚠️ `visual-compare.js` не имеет скрипта в `package.json` и порога прохождения —
всегда `exit 0`. Для CI непригоден как есть.

## Открытые хвосты этого слота
- `check-memory.sh` проверяет лимиты, индекс, ссылки, незапушенные коммиты и
  просроченный разбор inbox. **Не проверяет** соответствие статусов в `backlog.md`
  реальности — это следующий шаг (нужны машинные проверки под каждый DoD).
- Хук `pre-commit` с gitleaks не установлен (B-01, пункт 5 DoD).
- Ответы на Q1–Q5 из `backlog.md` не получены.

## Не трогать без разговора с владельцем
- `docs/project/CONTEXT.md` — содержит боевые ключи И читается скриптами
  (`mgts-backend/scripts/analyze-infoformen-accordion.js:146-156`). Порядок обязателен:
  сначала перевести скрипты на env, потом отзывать ключи, потом чистить историю.
- `mgts-backend/public/uploads/` и `temp/` — 2.9 ГБ, решение по ним — вопрос Q1.
