# Inbox — август 2026

Сырые находки, **в момент события**. Разбор — потом, батчем (раз в несколько дней).

## Правила ведения

- Пишем, **только если** владелец поправил, возразил, сказал «опять» или показал дефект
  на примере — либо ошибку поймала самопроверка или машина.
  Не поправил — не пишем: пустая запись хуже пропуска.
- 🔴 **Поле «поймал» обязательно и заполняется честно.** Назвал класс ПОСЛЕ замечания
  владельца — это `владелец`, а не `самопроверка`. Без этой честности метрика
  «сколько ошибок ловим сами» превращается в самообман.
- **В моменте не разбираем** — только фиксируем.

Формат:

```
**ДД.ММ ЧЧ:ММ** · слот `<слот>` · ветка `<ветка>` · поймал: **<кто>**

Что произошло — фактами, с числами и путями.
Класс: **К-NN — название.**
Что сделано / какое правило из этого следует.
```

---

**14.08 · слот `arch` · ветка `main` · поймал: **машина** (агент-исследователь)**

Четыре субагента, исследовавших репозиторий, в шапке отчётов указали разные корневые
пути: `/root/mgts`, `/home/claude/mgts`, `/Users/andrey_efremov/Downloads/runs`.
Я передал им путь `/home/claude/mgts`, не проверив, где клон оказался фактически.
Трое из четырёх это поймали и поправили сами, один построил отчёт по устройству
подключённой машины.
Класс: **К-06 — конфигурация принята за состояние** (я передал путь по памяти,
а не по выводу команды).
Следствие: в задании субагенту путь указывать только после `ls`, подтверждающего его.

---

**14.08 · слот `arch` · ветка `main` · поймал: **самопроверка****

Собирался вынести `docs/project/CONTEXT.md` в `_to_delete/` как файл с секретами.
Проверка потребителей показала, что файл **читается кодом**:
`mgts-backend/scripts/analyze-infoformen-accordion.js:146-156` парсит из него
`pplx-[A-Za-z0-9]+`. Простой перенос сломал бы пайплайн анализа.
Класс: **К-02 — правишь источник, не прошёл по потребителям.**
Следствие: порядок в B-01 зафиксирован явно — сначала env, потом отзыв, потом история.

---

**14.08 · слот `arch` · ветка `main` · поймал: **самопроверка****

Перенос файлов в `_to_delete/` **не удаляет секреты из истории git** и не отзывает
ключи. Если бы я отчитался «секреты вынесены», это был бы К-05 в чистом виде.
Класс: **К-05 — формальный признак принят за результат.**
Следствие: B-01 сформулирована как «отозвать», а не «убрать файлы», и её DoD требует
чистого прогона gitleaks по **истории**, а не по рабочей копии.

---

**14.08 · слот `arch` · ветка `main` · поймал: **владелец****

Сказал, что push невозможен. Проверил два канала: `device_bash` (`403 from proxy after
CONNECT`) и облачный контейнер (`not in this session's authorized repository set`).
Третий канал — Desktop Commander (`mcp__remote-devices__Desktop_Commander__*`), который
выполняется на самом macOS, — не проверил, хотя он был доступен всю сессию.
Владелец предложил «сделать бридж» — бридж уже стоял. Push прошёл с первой попытки:
`932e20e..abb9fc3  main -> main`, `EXIT=0`.
Класс: **К-13 — отрицательный результат по одному пути принят за отсутствие явления.**
Заведён новый класс К-13; каналы разобраны в `topics/agent-environment.md`.
Правило: git-операции по этому репозиторию — через Desktop Commander, не через `device_bash`.

---

**17.08 · слот `arch` · ветка `main` · поймал: **владелец****

Прямой вопрос: «ты зафиксировал эти планы в мемори банк?» Ответ — нет. Восемь ходов
подряд делал прототипы, планы, требования и валидацию, и **ничего не записал**.
Всё жило в переписке и в отданных файлах. Сессия закончилась бы — знание исчезло.
Класс: **К-10 — обещание без носителя.** Причём правило написано моей же рукой
в `CLAUDE.md`: «конец сессии → запись в inbox, обновление activeContext, коммит».
Следствие: фиксация проведена, но нужен носитель. Предложение — пункт в `check-memory.sh`:
ругаться, если `activeContext.md` старше последнего коммита в `docs/` или `design/`.

---

**17.08 · слот `arch` · ветка `main` · поймал: **владелец****

Вынес на публичную поверхность наши финансовые показатели вместо клиентской выгоды:
«+9–15 % / +24–36 % / +48–78 % к чеку объекта», ×4 рост PropTech, 65 % доли выручки.
Владелец: «а тут ожидается инфографика о том, что получает клиент».
Класс: **К-14 — свой показатель выдан за пользу клиента** (новый).
Перевёрнуто на −30–40 % к pre-construction, 0 ₽ аванса, 1 договор вместо 31 подрядчика.

---

**17.08 · слот `arch` · ветка `main` · поймал: **владелец****

Две итерации рисовал карту Москвы процедурно. Владелец: «это карта таро, сделанная
с багами… наверняка в интернете есть куча готовых решений». Взял OSM — узнаваемо сразу.
Класс: **К-15 — рисую от руки то, что есть в данных** (новый).

---

**17.08 · слот `arch` · ветка `main` · поймал: **владелец****

«Собрал ли ты требования к шаблонам для карты, точек и 3D?» — нет, не собирал.
Был прототип, то есть решения по ходу, а не согласованный набор требований.
Класс: **К-16 — прототип принят за требования** (новый).

---

**17.08 · слот `arch` · ветка `main` · поймал: **самопроверка****

При замере для плана интеграции пересчитал ветки `SectionRenderer` — их 29, а не 31,
как записано в `topics/frontend-render.md`. Цифру я взял из отчёта субагента и не проверил.
Класс: **К-08 — источник истины отстал от реальности**, но с обратным знаком: не документ
отстал от кода, а я записал в документ непроверенное число.
Исправлено в `topics/frontend-render.md` с пометкой.

---

**17.08 · слот `arch` · ветка `main` · закрыто машиной**

К-10 (восемь ходов работы без единой записи в память) текстовым правилом не ловится.
В `check-memory.sh` добавлена **секция 8**: сравнивает время последней правки
`docs design mgts-frontend/src mgts-backend/src scripts` со временем последней правки
`memory-bank/activeContext.md` — и по коммитам, и по грязному дереву.
`temp/`, `uploads/`, `.dev/` намеренно вне списка: шумят.

🔒 **Мутационный тест пройден, 4 из 4:**

| Мутация | Ожидание | Факт |
|---|---|---|
| правка `design/` без правок памяти | красный | ✗ «1 незакоммиченных правок… НИ ОДНОЙ в memory-bank», код возврата 1 |
| правка `design/` + правка памяти | зелёный | ✓ «работа идёт и память вместе с ней (1/1)» |
| коммит в `design/` позже `activeContext` | красный | ✗ «разрыв 1 ч.» + оба коммита показаны |
| коммит в `activeContext` следом | зелёный | ✓ отпускает |

Мутации делались на ветках `mutation/*`, ветки удалены, дерево чистое.

---

**17.08 · слот `arch` · стандарт планирования перенесён и проверен машиной**

Владелец принёс стандарт из проекта Investments (12 планов, 08.08). Перенёс
целиком: 13 принципов, скелет, два чек-листа, табло §0.0.

⚠️ **Сначала положил не туда.** Развернул стандарт в `implementation-plans/README.md`,
не проверив, как он называется в соседнем проекте. Владелец спросил — оказалось,
там `memory-bank/PLANNING-STANDARD.md`, 158 строк. Переложил под тем же именем:
агент, переходящий между проектами, должен искать стандарт по одному пути.
README папки планов вернулся к своей работе — реестр планов.
Класс — **К-01**: я решил задачу в границах примера («обнови стандарт»), не спросив
у реальности, есть ли у этой вещи уже имя и место.

Три вещи, которые пришлось делать, а не копировать:

1. **Номера классов чужие.** Их К-20 = наш К-10, их К-21 = наш К-05, их К-14
   (состояние по памяти) = наш К-08. А наш К-14 — совсем про другое, про свой
   показатель вместо клиентской пользы. Скопируй я ссылки как есть — инвариант
   сторожил бы не то, что написано. Таблица соответствия — §7 стандарта.
2. **Стандарт ссылался на два файла, которых у нас нет.** `patterns/coverage.md`
   создал: из 16 классов машину имеют **три**, семь назначены планом, шесть
   не наблюдаются ничем. `guard-autonomy.md` не выдумывал — B-30.
3. **Правило без машины — не правило.** Принципы 11 и 12 (откат у каждой фазы,
   закрывающий блок с «что НЕ доказано») положил в `check-memory.sh` §9.

🔒 **Мутации 4 из 4:** план без §0.0 → красный · фаза без отката → красный ·
закрытая фаза без «что НЕ доказано» → красный · всё по стандарту → зелёный.

⚠️ **Первая версия проверки давала ложный зелёный.** `grep -c ... || echo 0`
при нуле совпадений печатал «0\n0», сравнение падало с ошибкой, и ветка уходила
в `else` — то есть в «✓ у всех фаз назван откат» при нуле откатов. Это **К-09**,
ложное срабатывание собственного аудита, поймано первым же запуском. Мораль та же:
проверку саму нужно мутировать, иначе она врёт увереннее памяти.

PLAN-01 приведён к стандарту: у восьми спринтов появилась строка отката, два
из них помечены как необратимые (снос 68 дублей схем в Ф2, снятие 13 страниц в Ф6) —
именно поэтому в Ф6 страницы не снимаются, а получают баннер.

---

**17.08 · слот `arch` · PLAN-01 переоформлен по стандарту**

Плоский документ стал планом по скелету `PLANNING-STANDARD.md`: фронтматтер,
табло §0.0 со счётчиками, `## 0. Факты` с замером по коду, цель с пятью числами,
метод, **карта потребителей** (10 узлов, колонка «молча предполагает» — там и живут
тихие поломки), восемь фаз с четвёркой Артефакт · Метрика · Инвариант (класс) · Откат,
риски, `Q-R01.1…9`, сводка артефактов, реестр заглушек, DoD, 21 мутация.

Что дало само оформление, а не переписывание:

- **Инвариант заставил назвать класс.** У восьми фаз — К-02, К-04, К-05, К-07, К-09,
  К-11, К-14, К-16. К-16 до этого в `coverage.md` стоял ❌; теперь у него есть
  сторож в Ф5. Счётчики покрытия: ✅ 3 · 🟡 8 · ❌ 5.
- **Табло обнажило неприятное:** 0 ✅ из 21 замка. Меры описаны, ни одна не проверена
  мутацией — по правилу §6 стандарта это 🟡/⬜, а не ✅.
- **Карта потребителей** собрала в одном месте пять допущений, каждое из которых
  сегодня ложно: `populate=*` в Strapi 5, наличие классов Tailwind, вызов
  `revalidateTag`, статика как эталон, невозможность анонимного удаления.

⚠️ **Проверка планов снова врала, второй раз за день.** Секция 9 считала
**шаблон** закрывающего блока за закрытую фазу: «✓ у всех 1 закрытых фаз назван
недоказанный остаток» при нуле закрытых фаз. Опять **К-09**. Починено: строки
с `ДД.ММ` и «обязательное поле» из подсчёта исключены. Мутации 3 из 3 —
закрытая фаза без поля красная, с полем зелёная, один шаблон не считается.

Вывод, который стоит записать отдельно: **проверка, написанная под документ,
почти всегда сначала считает сам документ.** Мутировать её надо на том же документе,
ради которого писалась, а не на синтетическом.

---

## Следующий разбор

Плановая дата: **при накоплении ~10 записей** либо через 2 недели — что раньше.
Просрочку ловит `check-memory.sh` (порог 14 дней по дате изменения этого файла).
